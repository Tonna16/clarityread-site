{
  "name": "clarityread-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static website and serverless support endpoint for ClarityRead.",
  "scripts": {
    "test": "node --check netlify/functions/support.js"
  }
}
